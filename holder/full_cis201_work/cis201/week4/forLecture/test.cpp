#include <iostream>;

using namespace std;

int main()
{
    total = 0.0;
    input = 0;
    count = 0;

    while (input>= 0)
    {
    "enter a number"
    if (input >= 0)

        {
            total == total + input;
            count += 1;
        }    
    }
    cout << "the average is " << endl;   
   return 0;
}
